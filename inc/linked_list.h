#ifndef LINKED_LIST_H
#define LINKED_LIST_H

typedef LinkedList struct linked_list;

LinkedList *create_ll(int num);
/* Allocates space for a new linked list with each node having storage for num
 * elements.
 */

#endif //LINKED_LIST_H
